{
  "name": "lyric-flow",
  "private": true,
  "scripts": {
    "postinstall": "npm ci --prefix web",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "test": "npm --prefix web test",
    "test:e2e": "npm --prefix web run test:e2e",
    "typecheck": "npm --prefix web run typecheck",
    "lint": "npm --prefix web run lint",
    "format": "npm --prefix web run format",
    "format:check": "npm --prefix web run format:check",
    "build:static": "npm --prefix web run build:static",
    "preview:static": "npm --prefix web run preview:static",
    "test:static": "npm --prefix web run test:static"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
