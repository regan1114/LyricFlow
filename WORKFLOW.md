# 歌曲資料夾 → SRT、分鏡圖片、對齊 JSON 與專輯封面

這是供 Codex 或其他 AI 助手在此專案執行的製作規格。你把歌曲音檔與完整 Suno 歌詞放進同一資料夾，再提供資料夾路徑；AI 負責對時、規劃分鏡、生成各張圖片、專輯命名與封面及整理匯入檔。最後由你在 LyricFlow 調整視覺效果、預覽並匯出影片。

## 給接手的 AI

先讀根目錄 [AGENTS.md](AGENTS.md)，再讀本文件。`WORKFLOW.md` 定義製作規格；每首歌的輸入來源、使用者指定需求與實際進度分別保存在該歌曲資料夾內，不能只靠聊天記憶推測。

| 要確認的事情                     | 讀取位置                                                            |
| -------------------------------- | ------------------------------------------------------------------- |
| 要處理哪首歌                     | 使用者指定的資料夾；「我的歌曲」預設 `input/我的歌曲/`              |
| 原始音檔、歌詞、預設比例         | 資料夾內原始檔案，以及準備後的 `output/source.json`                 |
| 本次指定畫風、張數、名稱與進度   | `output/PROGRESS.md`                                                |
| 完整歌詞及分鏡行號               | `output/suno-lyrics.txt`、`output/lyrics-index.json`                |
| 對時工作與逐句時間               | `output/alignment-job.json`、`output/alignment.json`                |
| 分鏡安排、圖片生成方式及專輯名稱 | `output/storyboard.json`、`storyboard.md`、`prompts.md`、`album.md` |
| 最終交付項目                     | 本文件的「交付目錄」及下方完成條件                                  |

**需要的能力：** 讀寫指定資料夾、執行本機 Python 或呼叫本機 API，以及生成和檢視圖片。Codex 使用 `imagegen` 技能；其他 AI 可使用其環境中可用且獲授權的影像工具。沒有影像工具時，先完成可執行的檢查、對時與分鏡規劃，記錄缺少的能力，不將提示詞或拼貼圖當作已交付的獨立分鏡圖。

每完成一步，更新 `PROGRESS.md` 的勾選、成果路徑與下一步。接手時實際檢查檔案，不只看勾選；對時仍在執行就查詢既有工作，不重複提交。舊資料夾沒有 `PROGRESS.md` 時，依現存檔案補建紀錄，不重新執行會建立 `output/` 的準備指令。

**完成條件：** 所有原歌詞均有可靠的時間且完整出現在 SRT／JSON；JSON 可由現有匯出器驗證，所引用圖片確實存在且逐張檢視；方形封面與 `album.md` 的專輯名稱逐字一致；提示詞、待確認事項與匯入步驟均可查閱。尚有漏句、缺圖或無法確認的對時時，紀錄具體位置並如實交代完成範圍。

將工作交給不會自動讀取 `AGENTS.md` 的 AI 時，可貼這段：

> 請先閱讀此專案根目錄的 AGENTS.md 和 WORKFLOW.md，依規格處理「歌曲資料夾的完整路徑」。若 output 已存在，先讀 PROGRESS.md 與 source.json，核對來源後接續製作。請產出完整 SRT、獨立分鏡圖、依圖檔名稱對齊歌詞的 JSON，以及包含專輯名稱的方形封面；完成每一步請更新進度紀錄。

## 放檔即備妥輸入

已建立 `input/我的歌曲/` 作為第一首歌的放檔位置。每首歌使用獨立資料夾，可自行改名；資料夾內放一首音檔與一份 TXT 歌詞，檔名可以自訂。例如：

```text
input/我的歌曲/
  我的歌曲.mp3
  歌詞.txt
  專輯名稱.txt             # 選填；單行名稱，不提供則由 Codex 依歌詞命名
```

準備好後只需說：

> 請依 WORKFLOW.md 處理 input/我的歌曲，產出完整素材和專輯封面。

也可提供其他本機資料夾路徑。這是由 Codex 收到指令後執行的工作流，放入檔案本身不會啟動背景監控或自動生成圖片。每個資料夾只放一首歌，避免選錯音檔或歌詞版本；已有成品的資料夾可要求繼續製作。

## 每首歌只需提供

- 歌曲：MP3、WAV 或本機辨識支援的音檔，也可直接提供本機路徑。
- 完整歌詞：直接貼上，或提供 UTF-8 文字檔。可保留 `[Verse]`、`[Chorus]` 等 Suno 段落標記；重複副歌仍須完整列出。

未另行指定時，分鏡使用 **16:9 橫式、同一套人物與美術風格、圖片不含文字、下方保留字幕空間**；專輯封面使用 **1:1 方形、同系列美術風格，印上專輯名稱**。分鏡依歌詞情境與實際段落長度安排，不強制每句換圖；圖片數量在讀完歌詞後決定。也可額外指定直式、畫風、角色、張數或專輯名稱。聊天中提供的專輯名稱優先於檔案，未指定則由 Codex 根據歌詞命名並記錄簡短理由；不自行加入藝人名或其他封面文案。

若原先直接提供音檔與貼上的歌詞，可先整理成上述資料夾，再採用同一流程。

## 準備資料夾（由 Codex 執行）

在專案根目錄執行：

```sh
.venv/bin/python -m scripts.prepare_song_folder "input/我的歌曲"
```

此步會檢查單首音檔、單份歌詞、Suno 清理後的句數及選填專輯名稱，然後建立同資料夾內的 `output/`，保留原始歌詞、來源路徑、逐句編號及 `PROGRESS.md` 進度紀錄。音檔格式與大小沿用本機服務限制，音檔能否解碼與實際長度在對時階段檢查。不會複製或更改原音檔，不會產生空白字幕或假圖檔。

若 `output/` 已存在，準備指令會停止並保留內容。繼續製作時先核對 `source.json`、原始歌詞與音檔是否仍相同，再依已有工作編號查詢，沿用已完成的圖片；不要重複提交同一對時工作。換歌或修訂原始輸入時建立新資料夾，避免混用舊時間。

## 執行順序（由 Codex 完成）

1. **保存輸入與核對歌詞。** 使用準備指令建立 `<歌曲資料夾>/output/`，保留原始歌詞；對時使用專案既有的 Suno 標記清理規則，不改寫唱出的文字，也不省略重複段落。`lyrics-index.json` 是分鏡使用的歌詞行號。若歌詞只寫「副歌重複」而沒有實際內容，先補齊確切文字。
2. **用歌曲對時。** 使用本機 `http://127.0.0.1:8080` 與 `lyric_flow_client.py`，保存工作編號及完成後的工作 JSON。時間取自音檔辨識，不按字數平均分配，也不憑歌詞猜時間。可用 CLI `lyric_flow.py` 取得同格式的 alignment JSON。
3. **檢查完整性與時間。** 逐句比對清理後的原始歌詞、重複副歌、前奏／間奏／尾奏及歌曲長度。未定位句先透過 [補辨識 API](API.md#補辨識漏句) 重試；保留已定位時間。對 `review` 句與轉場試聽核對；若當前環境無法可靠確認，交付時明列待確認位置，不宣稱已驗證。不能把漏句刪掉、填空白或虛構歌詞來通過匯入。
4. **規劃分鏡。** 依原歌詞行號建立 `storyboard.json`，記錄每張圖從哪一句開始。另寫 `storyboard.md`，列出對應歌詞、畫面、角色及時間；副歌再現可以共用或生成有變化的新鏡頭。第一張涵蓋片頭，最後一張涵蓋片尾；無歌詞的間奏延續當時畫面。
5. **生成獨立圖檔。** 使用環境中可用的影像工具（Codex 使用 `imagegen` 技能），先確立角色與風格，再逐張生成、檢視並按需修正；後續圖可引用已確認的圖片保持一致。每個分鏡交付一張獨立圖片，不用一張分鏡拼貼圖代替。指定 16:9，檢查實際比例與構圖，確保人物不被字幕或畫面裁切遮住。保存提示詞、使用的影像工具與參考圖於 `prompts.md`，最終圖檔放入 `images/`，例如 `001.png`、`002.png`。
6. **專輯名稱與封面。** 使用指定的名稱或根據歌詞命名，把最終名稱與命名理由記錄在 `album.md`。以相同美術設定生成獨立的方形封面 `cover/album-cover.png`，提示詞逐字引用最終專輯名稱；檢視中文字、排列、邊界及縮小後的可讀性，發現錯字使用影像工具修正後重查。封面標題與 `album.md` 必須相同，不能將有錯字的版本當作完成品。封面不放進 `images/`，也不自動插入歌詞圖片編排。
7. **產生一致的字幕與編排。** 執行下方匯出指令；SRT 和圖片 JSON 必須由同一份完整對時資料產生。每句歌詞對應一個 JSON 段落，多句可共用同一 `name`。JSON 不含 `image` 或 Base64。
8. **交付並說明待確認項目。** 核對四類主要成果齊全：完整 SRT、每張分鏡圖、對齊 JSON、含專輯名稱的封面；另附分鏡表、提示詞、專輯名稱與必要的對時檢查說明。只有格式檢查及所有歌詞時間都齊全時，才稱為可匯入素材；聽感仍有疑慮時清楚列出。將交付路徑與封面預覽提供給使用者，未完成的項目不能以佔位圖或空檔交差。需要壓縮包時再將此資料夾打包。

生成圖片由 AI 助手的影像工具執行；以下 Python 指令負責檢查及匯出，不會自行呼叫影像生成服務。

## 交付目錄

```text
input/我的歌曲/output/
  images/
    001.png
    002.png
    ...
  cover/
    album-cover.png           # 方形專輯封面，包含專輯名稱
  album.md                    # 專輯名稱與命名理由
  lyrics.srt                  # 全部歌詞的字幕時間
  image-subtitles.json        # 給 LyricFlow 匯入，依 name 對應素材
  storyboard.md               # 分鏡與歌詞對照
  storyboard.json             # 生成編排用的分鏡起點
  prompts.md                  # 分鏡與封面的提示詞、共用風格
  alignment.json              # 完整對時結果與檢查資訊
  suno-lyrics.txt             # 原始歌詞
  lyrics-index.json           # 清理標記後的歌詞編號
  source.json                 # 來源音檔、歌詞與預設比例
  PROGRESS.md                 # 使用者指定需求、完成項目與接手的下一步
  alignment-job.json          # 已提交的對時工作編號，提交後立即保存
```

音檔沿用你提供的原始檔案，不必額外複製一份。專案內的 `input/` 及 `output/` 已排除於 Git 追蹤。

## 取得對時資料

使用既有客戶端提交工作後，立即保存 `job["id"]`，再查詢進度，避免等待逾時後重複提交：

```python
import json
from pathlib import Path
from lyric_flow_client import LyricFlowClient

folder = Path("input/我的歌曲/output")
source = json.loads((folder / "source.json").read_text(encoding="utf-8"))
client = LyricFlowClient()
job = client.submit(source["audio"], (folder / "suno-lyrics.txt").read_text(encoding="utf-8-sig"))
(folder / "alignment-job.json").write_text(json.dumps(job), encoding="utf-8")
# 之後可透過已保存的工作編號繼續查詢；完成前不可進行素材匯出。
completed = client.wait(job["id"], timeout=1800)
(folder / "alignment.json").write_text(
    json.dumps(completed, ensure_ascii=False, indent=2), encoding="utf-8"
)
```

開始前先完成資料夾準備；本機服務須已啟動且引擎就緒。若需要補辨識，保存新工作編號並以補辨識完成的結果更新 `alignment.json`。僅在瀏覽器修改的字幕時間不會自動同步到此檔案。

## 分鏡起點與匯出

`storyboard.json` 是陣列，`fromLine` 為**清理 Suno 標記後、從 1 開始的歌詞行號**，不是原始文字檔行號，也不是秒數。第一張從第 1 句起，後續起點嚴格遞增；最後一張一路使用到最後一句。例如以下僅展示有至少 9 句歌詞時的格式：

```json
[
  { "name": "001.png", "fromLine": 1 },
  { "name": "002.png", "fromLine": 5 },
  { "name": "003.png", "fromLine": 9 }
]
```

在專案根目錄執行（由 Codex 代為執行即可）：

```sh
.venv/bin/python -m scripts.package_storyboard \
  --alignment "input/我的歌曲/output/alignment.json" \
  --lyrics "input/我的歌曲/output/suno-lyrics.txt" \
  --storyboard "input/我的歌曲/output/storyboard.json" \
  --output "input/我的歌曲/output"
```

匯出器接受 API 完成工作 JSON 或 CLI 的 `*.alignment.json`。它會檢查：歌詞全文與行序一致、無空白或未定位句、時間至少 0.05 秒且不重疊、不超出音檔長度、分鏡從第 1 句開始、圖檔存在且檔名完全一致。既有 SRT／圖片 JSON 不會直接覆寫；修訂時先保留舊版再重建。

每首最多 500 個歌詞段落，與目前圖片字幕匯入限制相同。格式檢查不等於聽感對時驗證。

## 你在 LyricFlow 的操作

1. 匯入原始歌曲，先讓音軌具有正確的完整長度。
2. 將 `images/` 內所有圖檔加入素材庫，保留原檔名，避免同名素材。
3. 匯入 `image-subtitles.json`；它同時帶入完整字幕與圖片排列，無需再匯入一次 SRT。單獨的 `lyrics.srt` 可留作備份或用於其他剪輯工具。
4. 預覽分鏡與字幕，選擇字型、效果及輸出設定，再匯出影片。

`cover/album-cover.png` 是另外交付的專輯封面，可用於發布素材。需要將封面放進影片時，再自行加入時間軸，或告訴 Codex 要在哪個段落使用。

現有工具會將第一張圖片貼齊 0 秒，每張持續到下一張的切換起點，最後一張延伸至歌曲結尾。字幕仍使用原本歌詞時間，間奏不添加假字幕。同圖多句會產生相鄰片段，但使用相同素材；JSON 段落數可以多於圖片數。

圖片 JSON 匯入會取代目前圖片軌與字幕。若之後改換更長的音檔，請在音軌更新後重新匯入 JSON，重新計算最後一張的結束位置。
